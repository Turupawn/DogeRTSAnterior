<%= form_for(@wow_upgrade) do |f| %>
  <% if @wow_upgrade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wow_upgrade.errors.count, "error") %> prohibited this wow_upgrade from being saved:</h2>

      <ul>
      <% @wow_upgrade.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :building_id %><br>
    <%= f.number_field :building_id %>
  </div>
  <div class="field">
    <%= f.label :type %><br>
    <%= f.text_field :type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
