<%= form_for(@much_withdraw) do |f| %>
  <% if @much_withdraw.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@much_withdraw.errors.count, "error") %> prohibited this much_withdraw from being saved:</h2>

      <ul>
      <% @much_withdraw.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :building_id %><br>
    <%= f.number_field :building_id %>
  </div>
  <div class="field">
    <%= f.label :timestamp %><br>
    <%= f.datetime_select :timestamp %>
  </div>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.number_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
